<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center"
    android:background="@color/white">

    <!-- Knets Jr Logo/Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔐 Knets Jr Security"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/primary_color"
        android:layout_marginBottom="32dp"
        android:gravity="center" />

    <!-- Verification Message -->
    <TextView
        android:id="@+id/tvVerificationMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Enter your 4-digit secret code to disable parental controls:"
        android:textSize="16sp"
        android:textColor="@color/text_dark"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <!-- Secret Code Input -->
    <EditText
        android:id="@+id/etSecretCodeVerify"
        android:layout_width="200dp"
        android:layout_height="56dp"
        android:hint="Enter Secret Code"
        android:inputType="numberPassword"
        android:maxLength="4"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:background="@drawable/edittext_background"
        android:layout_marginBottom="24dp"
        android:textColor="@color/text_dark" />

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center">

        <Button
            android:id="@+id/btnCancelDisable"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Cancel"
            android:textColor="@color/text_dark"
            android:background="@drawable/button_secondary"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/btnVerifySecret"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Verify Code"
            android:textColor="@color/white"
            android:background="@drawable/button_primary"
            android:layout_marginStart="8dp"
            android:enabled="false" />

    </LinearLayout>

    <!-- Warning Text -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="⚠️ Disabling parental controls will remove all monitoring and restrictions from this device."
        android:textSize="12sp"
        android:textColor="@color/warning_color"
        android:gravity="center"
        android:layout_marginTop="24dp"
        android:padding="12dp"
        android:background="@drawable/warning_background" />

</LinearLayout>